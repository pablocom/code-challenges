//! Data structures built from scratch: a binary max-heap, an LRU cache, a
//! min-stack, a queue from two stacks, and a task scheduler that reuses the
//! heap.

pub mod lru_cache;
pub mod max_heap;
pub mod min_stack;
pub mod stack_based_queue;
pub mod task_scheduler;
